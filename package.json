{
  "name": "vibemoji-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Top-level scripts for building and running vibemoji's core (web) and desktop shells.",
  "scripts": {
    "web-dev": "npm --prefix core run dev",
    "web-build": "npm --prefix core run build",
    "desktop-dev": "npm --prefix desktop run dev",
    "desktop-start": "npm --prefix desktop start",
    "desktop-build": "npm --prefix core run build && npm --prefix desktop run build:win",
    "desktop-run": "npm run web-build && npm --prefix desktop run sync-core && npm --prefix desktop start",
    "android-sync": "npm run web-build && npm --prefix android run sync-core && npm --prefix android run sync",
    "android-dev": "npm --prefix android run dev",
    "android-open": "npm --prefix android run open",
    "android-build": "npm run android-sync && npm --prefix android run build",
    "install-all": "npm --prefix core install && npm --prefix desktop install && npm --prefix android install"
  }
}
