<!--
  Manifest fragments injected into android/app/src/main/AndroidManifest.xml by
  install-overlay.js. The script:
    - inserts the <uses-permission> entries just before the <application> tag
    - inserts the <service> entry just before </application>
-->

<!-- @permissions -->
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

<!-- @service -->
<service
    android:name=".OverlayService"
    android:exported="false"
    android:foregroundServiceType="specialUse">
    <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="floating_overlay_buddy" />
</service>
